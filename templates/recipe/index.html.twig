{% extends 'base.html.twig' %}

{% block title 'Recettes' %}

{% block body %}
    <h1>Recettes rapides</h1>
    <ul>
        {%  for recipe in shorts %}
            <li>
                <a href="{{ path('recipe.show', {id: recipe.id, slug: recipe.slug}) }}">{{ recipe.title }}</a>
            </li>
        {%  endfor %}
    </ul>
    <h1>Liste des recettes</h1>
    <ul>
        {%  for recipe in recipes %}
        <li>
            <a href="{{ path('recipe.show', {id: recipe.id, slug: recipe.slug}) }}">{{ recipe.title }}</a>
        </li>
        {%  endfor %}
    </ul>
{% endblock %}
